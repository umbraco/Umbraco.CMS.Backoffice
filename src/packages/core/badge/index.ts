export { UMB_BADGE_CONTEXT } from './badge.context-token.js';
export { UmbBadgeContext } from './badge.context.js';

export interface UmbBadge {
	unique: string;
	content: string;
}

import './badge-slot.element.js';
